appId: app.axross.nakami
name: app-launch — cold start reaches the welcome screen
tags:
  - scenario:app.launch
  - area:app
  - priority:must
---
- launchApp:
    clearState: true
- assertVisible:
    id: "welcome-screen"
- assertVisible: "Connect to Payload"
