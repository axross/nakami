# Autogenerated-style Fastlane plugin list, pulled in by the root Gemfile.
# firebase_app_distribution uploads the signed preview APK and returns the
# tester install link surfaced by android-build.yml.
gem "fastlane-plugin-firebase_app_distribution", "~> 1.0"
